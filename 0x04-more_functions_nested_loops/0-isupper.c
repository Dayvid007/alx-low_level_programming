#include "main.h"
/**
 *_isupper - prints alphabets in upper case
 *@c:  is the character to be checked
 *Return: returns 1 if successful, else returns 0
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')

	return (1);
	else
		return (0);
}
