#include "main.h"
/**
 *_isdigit - prints numbers in upper case
 *@c:  is the int to be checked
 *Return: returns 1 if successful, else returns 0
 */
int _isdigit(int c)
{
	if (c >= '0' && c <= '9')

	return (1);
	else
		return (0);
}
